import { GFS_Didot, Orbitron } from "next/font/google";


export const didot = GFS_Didot({ subsets: ["greek"], variable: "--font-didot", weight: "400", style: "normal" });

export const orbitron = Orbitron({ subsets: ["latin"], variable: "--font-orbitron" });


